umask 022
fsck -a /
setdate
remount -n / rw
prtroot >/etc/mtab
#Uncomment if you have multiple file systems
#fsck -a
#mount -a
>/var/run/utmp

if [ -x /sbin/audio-mixerd ] && [ -c /dev/audio ] && [ -c /dev/audio0 ]; then
	/sbin/audio-mixerd </dev/null >/dev/null 2>&1 &
fi
